Weather crud that makes use of two different api, one from a free wearher API and another from a API making use of a Firebase database.
Idea is to publish this angular front end as app on a phone, the back end is no longer needed due to Firebase API taking its role. 

css and html is not perfect yet, might come back to fix it, but on to new things :)
